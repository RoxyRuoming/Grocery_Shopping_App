// login return type
export type LoginResponseType = {
    success: boolean;
    data: {
        token: string;
    }
}

// register return type
export type RegisterResponseType = {
    success: boolean;
    data: boolean;
}

